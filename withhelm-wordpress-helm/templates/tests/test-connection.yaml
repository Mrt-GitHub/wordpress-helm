apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "withhelm-wordpress-helm.fullname" . }}-test-connection"
  labels:
    {{- include "withhelm-wordpress-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "withhelm-wordpress-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
